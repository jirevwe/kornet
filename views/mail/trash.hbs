
<div class="container">
    <div class="list-group">
        {{# each messages }}
            <a href="/mail/trash/{{this._id}}" class="list-group-item">
                <h4 class="list-group-item-heading">{{this.subject}}</h4>
                <p class="list-group-item-text">To: {{this.to.text}}</p>
                <p class="list-group-item-text"> Date: {{this.date}}</p>
            </a>
        {{/each}}
    </div>
</div>
<script> 
     $.get("http://localhost:3000/mail/refresh/trash", function(data, status, xhr){
        console.log(data);
        console.log(status);
    });
</script>